package com.horstmann.impatient

import com.horstmann.HorstmannConstants // you need this to work!

object UseConstants extends App {
  println(com.ComConst.name)
  println(HorstmannConstants.name)
}
